### Functional Requirements
1. Parent and Child checkboxes
2. Click on Parent checkbox should also check the Child checkboxes
3. Click on Child checkbox should only check the child and parent with square box
4. If all Childs are checked then automatically the Parent should get checked

## Non-Functional Requirements